const fetch = require("node-fetch") || global.fetch;

async function run() {
  const apiKey = process.env.GEMINI_API_KEY;
  // Let's just create an operation 
  // Wait, I don't have a valid URI to download now.
}
run();
